// TIL: buffer is just an array of bytes
// is it a reference to, or can it act on?
// i.e. necessarily &[u8], or can be [u8] ?
fn fixed_xor(buffer1: type, buffer2: type) {
    // not sure if len is right?
    // also, since buffer - any datatype?
    // so <T> as type?
    assert_eq!(buffer1.len(), buffer2.len()); 
    


}

fn main() {
    fixed_xor(b1, b2);
}
